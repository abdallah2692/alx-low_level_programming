#include "main.h"
/**
 * print_chessboard - print chaes board
 * @a: numof rows
 * Return: Nothing
*/

void print_chessboard(char (*a)[8])
{
	int A, b;

	for (A = 0; A < 0; A++)
	{
		for (b = 0; b < 0; b++)
		{
			_putchar(A[a][b]);
		}
		_putchar('\n');
	}
}
