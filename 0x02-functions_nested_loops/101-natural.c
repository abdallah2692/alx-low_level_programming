#include "main.h"

/**
 * main - Entry point
 * Description: computethe sum
 * Return: 0
 */

int main(void)
{
	int sum, num;

	for (num = 0; num < 10; num++)
	{
		if ((num % 3 == 0) || (num %5 == 0)
				sum += num;
	}
	printf("%d\n", sum);
	
	return(0);
}
